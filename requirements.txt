fastapi==0.115.12
uvicorn[standard]==0.34.0
discord.py==2.5.2
websockets==14.2
python-dotenv==1.1.0
pydantic==2.11.2
python-multipart==0.0.20

# --- v3 additions ---
aiosqlite==0.20.0
openai==1.68.2
twilio==9.5.0
httpx==0.28.1

# --- v5.1: Universal multi-provider LLM routing ---
# LiteLLM routes to 100+ providers (OpenAI, Anthropic, Gemini, Groq, Mistral,
# Ollama, Bedrock, Together AI, etc.) with a single unified interface.
litellm>=1.40.0
langgraph>=0.2.0

# --- AgentScope integration ---
# AgentScope provides MsgHub collaboration, ListMemory, ReActAgent tool loops,
# and ServiceToolkit for pytest/bandit/git as LLM-callable services.
# Install separately if not present: pip install agentscope>=0.0.6
agentscope>=0.0.6
